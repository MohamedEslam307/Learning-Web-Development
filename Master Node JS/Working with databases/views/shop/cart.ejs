<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%=pageTitle%></title>
    <link rel="stylesheet" href="/css/navbar.css">
    <link rel="stylesheet" href="/css/show-products.css">
    
</head>
<body>
    <%- include('../includes/navbar.ejs') %>
    <br><br><br>
    <main>
        <h1 style="text-align: center; font-size: 2em;">Cart Items</h1>
        <hr>
        <div class="product-grid">
            <% if (products.length === 0) { %>
                <p>No products found.</p>
            <% } else { for (let product of products) {%>
                <div class="product">
                        <h2><%=product.title%></h2>
                        <img src="<%=product.imageUrl%>" alt="<%=product.title%> image">
                        <p>Price: $<%=product.price%></p>
                        <p>Quantity: <%=product.quantity%></p>
                        <div style="text-align: center;">
                            <form action="/shop/product-details/<%=product.id%>" method="GET" style="display: inline;">
                                <button type="submit">Show Details</button>
                            </form>
                            <form action="/shop/cart/delete-product/<%=product.id%>" method="GET" style="display: inline;">
                                <button type="submit" style="background-color: rgb(153, 0, 0);">Delete Product</button>
                            </form>
                            <form action="/shop/cart/delete-item/<%=product.id%>" method="GET" style="display: inline;">
                                <button type="submit" style="background-color: rgb(112, 41, 0);">Delete item</button>
                            </form>
                        </div>
                </div>
                <% } %>
            <% } %>
        </div>
        <hr>
        <div style="text-align: center; font-size: 1.5em;">
            <h2>Total Price: $<%= totalPrice %></h2>
        </div>
        <div style="text-align: center;">
            <form action="/shop/checkout" method="POST" style="display: inline;">
                <button type="submit" style="display: inline-block; width: auto; padding: 15px 20px; margin-top: 10px; background-color: #007bff; color: #fff; border: none; border-radius: 6px; cursor: pointer; font-size: 1.2em;">Checkout</button>
            </form>
        </div>
    </main>
</body>
</html>
